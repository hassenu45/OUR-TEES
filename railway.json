{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx prisma generate && node scripts/fix-prisma-imports.mjs"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && node scripts/migrate_jod.mjs && node seed.mjs && node server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
